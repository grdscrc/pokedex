<%= link_to '<', root_path %>

<%= form_for :pokemon do |pk| %>
    <% Pokemon.column_names.each do |col| %>
        <% next if %w(id created_at updated_at).include? col %>
        <%= col %>: <%= pk.text_field col %><br />
    <% end %>
    <%= pk.submit %>
<% end %>